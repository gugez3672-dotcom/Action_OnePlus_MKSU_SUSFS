name: Build PLK110 SukiSU 4.2 + SUSFS 2.3

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - .github/plk110-build-trigger

permissions:
  contents: read

concurrency:
  group: plk110-susfs-build
  cancel-in-progress: false

env:
  DEVICE: PLK110
  SOC: sm8850
  CODENAME: canoe
  ANDROID_VERSION: android16
  KERNEL_VERSION: "6.12"
  BUILD_VARIANT: perf
  BUILD_NUMBER: "14541642"
  FIRMWARE_BASELINE: PLK110_16.0.8.302(CN01)
  EXPECTED_KERNEL_RELEASE: 6.12.23-android16-5-gb2a876903b49-ab14541642-4k
  STOCK_CONFIG_SHA256: e239b50cb4c660493edfdc2ae4908b56cf5cae1e826f71f637da85e4aea3ecd0
  EXPECTED_CONFIG_ADDITIONS_SHA256: d3235c8aa6e95b7b56c24a4619f01d930ffbac48a050d593fdcf896f770a997e
  A67_SELINUX_PATCH_SHA256: 101d7bce48d848495ae4d972347bfd60cd78f45dcc9bb4f6780162612c51f97c
  SUKISU_COMPAT_PATCH_SHA256: ea2be02443d0d5aed8da7866071af33bfc89a3f394d905b2db49798e6c574444

  # Exact running A.67 GKI commit plus the official A.67 OnePlus SoC/modules
  # snapshots. The public manifest is branch-based, so all three are checked
  # out to immutable commits below.
  MANIFEST_COMMIT: ecdd4809cbd01fa5f509a4708347026f63498303
  COMMON_COMMIT: b2a876903b495c444a94b16f50d1463ffe953957
  SOC_COMMIT: c88c1b3f75f7e88cd651608362ac7b95f11eb400
  MODULES_COMMIT: f45a4a587a0ba9107a5421a2dd978404f545164b

  # SukiSU's builtin branch contains the SUSFS-side SukiSU integration.
  SUKISU_COMMIT: e2912817f4e1b194e582a06e0b5eacf6a3fb7083
  SUSFS_COMMIT: c7db8cb367ef3ed4126ed7e222d77757ccb5c05b
  ANYKERNEL_COMMIT: 0fc7569866bb5d5fe51752cc410dca3e92ab332f

  SUSFS_MODULE_URL: https://github.com/sidex15/susfs4ksu-module/releases/download/v1.5.2%2B_R28/ksu_module_susfs_1.5.2%2B.zip
  SUSFS_MODULE_SHA256: abcd6bf456ea8189298d9e496faa440201d90e4f0bd93b83c0527589001c9376
  REPO_LAUNCHER_SHA256: 1211b57b57e4122a9c546295a59b37d24068f1164d0e87bef096d5323c413e4f

jobs:
  build:
    runs-on: ubuntu-24.04
    timeout-minutes: 180

    steps:
      - name: Maximize build space
        uses: easimon/maximize-build-space@c28619d8999a147d5e09c1199f84ff6af6ad5794
        with:
          root-reserve-mb: 8192
          temp-reserve-mb: 2048
          swap-size-mb: 8192
          remove-dotnet: "true"
          remove-android: "true"
          remove-haskell: "true"
          remove-codeql: "true"

      - name: Checkout audited build definition
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          persist-credentials: false

      - name: Verify checked-in baseline and adaptation
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "$GITHUB_SHA"
          echo "$STOCK_CONFIG_SHA256  baselines/PLK110_16.0.8.302_stock.config" | sha256sum --check
          echo "$EXPECTED_CONFIG_ADDITIONS_SHA256  baselines/PLK110_16.0.8.302_expected_sukisu_susfs.config.additions" | sha256sum --check
          echo "$A67_SELINUX_PATCH_SHA256  patches/PLK110_A67_susfs_selinux_hooks.patch" | sha256sum --check
          echo "$SUKISU_COMPAT_PATCH_SHA256  patches/PLK110_SukiSU_4.2_6.12_compat.patch" | sha256sum --check

      - name: Install dependencies
        run: |
          set -euo pipefail
          sudo apt-get update
          sudo apt-get install -y binutils git curl jq python3 zip

          curl --fail --location --retry 3 \
            https://storage.googleapis.com/git-repo-downloads/repo \
            --output /tmp/repo
          echo "$REPO_LAUNCHER_SHA256  /tmp/repo" | sha256sum --check
          sudo install -m 0755 /tmp/repo /usr/local/bin/repo

      - name: Sync and pin OnePlus source
        run: |
          set -euo pipefail
          mkdir kernel_workspace
          cd kernel_workspace

          repo init \
            -u https://github.com/OnePlusOSS/kernel_manifest.git \
            -b "$MANIFEST_COMMIT" \
            -m oneplus_15.xml \
            --depth=1

          # These are Android test-suite prebuilts, not kernel build inputs.
          # Their CodeLinaro branch was removed after OnePlus published the
          # manifest, so keeping them makes an otherwise valid source sync fail.
          mkdir -p .repo/local_manifests
          printf '%s\n' \
            '<?xml version="1.0" encoding="UTF-8"?>' \
            '<manifest>' \
            '  <remove-project name="kernel_platform/prebuilts/asuite" />' \
            '  <remove-project name="kernel_platform/tools/tradefederation/prebuilts" />' \
            '</manifest>' \
            > .repo/local_manifests/prune-unused-test-prebuilts.xml

          repo sync -c --no-tags --no-clone-bundle -j2

          git -C kernel_platform/common fetch --depth=1 origin "$COMMON_COMMIT"
          git -C kernel_platform/common checkout --detach "$COMMON_COMMIT"
          git -C kernel_platform/soc-repo fetch --depth=1 origin "$SOC_COMMIT"
          git -C kernel_platform/soc-repo checkout --detach "$SOC_COMMIT"
          git fetch --depth=1 origin "$MODULES_COMMIT"
          git checkout --detach "$MODULES_COMMIT"

          test "$(git -C kernel_platform/common rev-parse HEAD)" = "$COMMON_COMMIT"
          test "$(git -C kernel_platform/soc-repo rev-parse HEAD)" = "$SOC_COMMIT"
          test "$(git rev-parse HEAD)" = "$MODULES_COMMIT"
          grep -qx 'CONFIG_LOCALVERSION="-4k"' kernel_platform/common/arch/arm64/configs/gki_defconfig
          grep -Fq 'suffix = "-ab" + os.environ["BUILD_NUMBER"]' \
            kernel_platform/build/kernel/kleaf/workspace_status_stamp.py

      - name: Restore A.67 vmlinux_oki build output
        run: |
          set -euo pipefail
          cd kernel_workspace/kernel_platform/common

          # The pinned A.67 common commit predates OnePlus' vmlinux_oki copy,
          # while the pinned Kleaf build description already declares that
          # file as a required GKI output. Add only the later official copy
          # operation; the linked vmlinux itself is left untouched.
          grep -Fq '"vmlinux_oki",' ../build/kernel/kleaf/constants.bzl
          ! grep -Fq '$(Q)cp $(objtree)/vmlinux $(objtree)/vmlinux_oki' scripts/Makefile.vmlinux

          python3 - <<'PY'
          from pathlib import Path

          path = Path("scripts/Makefile.vmlinux")
          text = path.read_text()
          anchor = (
              "vmlinux: scripts/link-vmlinux.sh vmlinux.o $(KBUILD_LDS) FORCE\n"
              "\t+$(call if_changed_dep,link_vmlinux)\n"
          )
          replacement = anchor + "\t$(Q)cp $(objtree)/vmlinux $(objtree)/vmlinux_oki\n"
          if text.count(anchor) != 1:
              raise SystemExit("unexpected A.67 Makefile.vmlinux link rule")
          path.write_text(text.replace(anchor, replacement, 1))
          PY

          grep -Fq '$(Q)cp $(objtree)/vmlinux $(objtree)/vmlinux_oki' scripts/Makefile.vmlinux
          git diff --check
          test "$(git diff --numstat -- scripts/Makefile.vmlinux | awk '{print $1 " " $2}')" = "1 0"

      - name: Integrate pinned SukiSU builtin
        run: |
          set -euo pipefail
          cd kernel_workspace/kernel_platform

          git clone --filter=blob:none --no-checkout --depth=1 --branch builtin \
            https://github.com/SukiSU-Ultra/SukiSU-Ultra.git KernelSU
          git -C KernelSU fetch --depth=1 origin "$SUKISU_COMMIT"
          git -C KernelSU checkout --detach "$SUKISU_COMMIT"
          test "$(git -C KernelSU rev-parse HEAD)" = "$SUKISU_COMMIT"

          ln -sfn ../../KernelSU/kernel common/drivers/kernelsu
          grep -q 'obj-$(CONFIG_KSU) += kernelsu/' common/drivers/Makefile || \
            printf '\nobj-$(CONFIG_KSU) += kernelsu/\n' >> common/drivers/Makefile
          grep -q 'source "drivers/kernelsu/Kconfig"' common/drivers/Kconfig || \
            sed -i '/endmenu/i source "drivers/kernelsu/Kconfig"' common/drivers/Kconfig

          grep -q 'config KSU_SUSFS' KernelSU/kernel/Kconfig
          grep -q 'susfs_init();' KernelSU/kernel/ksu.c

      - name: Apply SukiSU 4.2 Android 6.12 compatibility patch
        run: |
          set -euo pipefail
          patch="$GITHUB_WORKSPACE/patches/PLK110_SukiSU_4.2_6.12_compat.patch"
          echo "$SUKISU_COMPAT_PATCH_SHA256  $patch" | sha256sum --check

          cd kernel_workspace/kernel_platform/KernelSU
          git apply --check "$patch"
          git apply "$patch"
          git diff --check

          grep -Fq 'static int kernel_umount_feature_set(u64 value)' kernel/feature/kernel_umount.c
          grep -Fq '.id = LSM_ID_UNDEF,' kernel/hook/lsm_hook.c
          grep -Fq 'security_add_hooks(ksu_hooks, ARRAY_SIZE(ksu_hooks), &ksu_lsmid);' kernel/hook/lsm_hook.c
          grep -Fq '#define LSM_ID_UNDEF' ../common/include/uapi/linux/lsm.h
          grep -Fq 'const struct lsm_id *lsmid' ../common/include/linux/lsm_hooks.h

      - name: Apply pinned SUSFS kernel patch
        run: |
          set -euo pipefail
          cd kernel_workspace

          git clone --filter=blob:none --no-checkout --depth=1 --branch gki-android16-6.12 \
            https://gitlab.com/simonpunk/susfs4ksu.git susfs4ksu
          git -C susfs4ksu fetch --depth=1 origin "$SUSFS_COMMIT"
          git -C susfs4ksu checkout --detach "$SUSFS_COMMIT"
          test "$(git -C susfs4ksu rev-parse HEAD)" = "$SUSFS_COMMIT"

          cp susfs4ksu/kernel_patches/fs/* kernel_platform/common/fs/
          cp susfs4ksu/kernel_patches/include/linux/* kernel_platform/common/include/linux/
          cp \
            susfs4ksu/kernel_patches/50_add_susfs_in_gki-android16-6.12.patch \
            kernel_platform/common/50_add_susfs.patch

          cd kernel_platform/common
          # OnePlus A.67 has a different comment block immediately after
          # selinux_state. Apply the upstream SUSFS patch everywhere else,
          # then use the audited A.67-only hooks.c context adaptation.
          git apply --check --exclude=security/selinux/hooks.c 50_add_susfs.patch
          git apply --exclude=security/selinux/hooks.c 50_add_susfs.patch
          git apply --check "$GITHUB_WORKSPACE/patches/PLK110_A67_susfs_selinux_hooks.patch"
          git apply "$GITHUB_WORKSPACE/patches/PLK110_A67_susfs_selinux_hooks.patch"

          grep -q '#define SUSFS_VERSION "v2.3.0"' include/linux/susfs.h
          grep -q 'obj-$(CONFIG_KSU_SUSFS) += susfs.o' fs/Makefile

      - name: Configure explicit SukiSU/SUSFS build
        run: |
          set -euo pipefail
          cd kernel_workspace/kernel_platform
          config=common/arch/arm64/configs/gki_defconfig

          ksu_kconfig=common/drivers/kernelsu/Kconfig
          test -f "$ksu_kconfig"

          default_of() {
            awk -v wanted="$1" '
              $1 == "config" && $2 == wanted { in_symbol=1; next }
              in_symbol && $1 == "default" { print $2; exit }
              in_symbol && $1 == "config" { exit }
            ' "$ksu_kconfig"
          }

          # Assert the pinned upstream semantics, but do not rely on defaults
          # for the effective build configuration. OnePlus' config pipeline can
          # normalize unset symbols away, which previously produced KSU=n.
          for symbol in \
            KSU KSU_SUSFS KSU_SUSFS_SUS_PATH KSU_SUSFS_SUS_MOUNT \
            KSU_SUSFS_SUS_KSTAT KSU_SUSFS_SPOOF_UNAME \
            KSU_SUSFS_HIDE_KSU_SUSFS_SYMBOLS \
            KSU_SUSFS_SPOOF_CMDLINE_OR_BOOTCONFIG \
            KSU_SUSFS_OPEN_REDIRECT KSU_SUSFS_SUS_MAP; do
            test "$(default_of "$symbol")" = y
          done
          test "$(default_of KSU_DEBUG)" = n
          test "$(default_of KPM)" = n

          # Remove stale KSU/KPM choices and write the exact audited effective
          # profile explicitly. This survives olddefconfig/savedefconfig instead
          # of depending on Kconfig default propagation.
          sed -i -E '/^(# )?CONFIG_(KSU|KPM)(_|=| is not set)/d' "$config"
          awk '
            /^CONFIG_EXT4_FS=y$/ {
              print "# CONFIG_KPM is not set"
              print "# CONFIG_KSU_DEBUG is not set"
              print "# CONFIG_KSU_FEATURE_ADBROOT is not set"
              print "# CONFIG_KSU_SUSFS_ENABLE_LOG is not set"
              print "CONFIG_KSU=y"
              print "CONFIG_KSU_SUSFS=y"
              print "CONFIG_KSU_SUSFS_HIDE_KSU_SUSFS_SYMBOLS=y"
              print "CONFIG_KSU_SUSFS_OPEN_REDIRECT=y"
              print "CONFIG_KSU_SUSFS_SPOOF_CMDLINE_OR_BOOTCONFIG=y"
              print "CONFIG_KSU_SUSFS_SPOOF_UNAME=y"
              print "CONFIG_KSU_SUSFS_SUS_KSTAT=y"
              print "CONFIG_KSU_SUSFS_SUS_MAP=y"
              print "CONFIG_KSU_SUSFS_SUS_MOUNT=y"
              print "CONFIG_KSU_SUSFS_SUS_PATH=y"
            }
            { print }
          ' "$config" > "$config.tmp"
          mv "$config.tmp" "$config"

          for line in \
            '# CONFIG_KPM is not set' \
            '# CONFIG_KSU_DEBUG is not set' \
            '# CONFIG_KSU_FEATURE_ADBROOT is not set' \
            '# CONFIG_KSU_SUSFS_ENABLE_LOG is not set' \
            'CONFIG_KSU=y' \
            'CONFIG_KSU_SUSFS=y' \
            'CONFIG_KSU_SUSFS_HIDE_KSU_SUSFS_SYMBOLS=y' \
            'CONFIG_KSU_SUSFS_OPEN_REDIRECT=y' \
            'CONFIG_KSU_SUSFS_SPOOF_CMDLINE_OR_BOOTCONFIG=y' \
            'CONFIG_KSU_SUSFS_SPOOF_UNAME=y' \
            'CONFIG_KSU_SUSFS_SUS_KSTAT=y' \
            'CONFIG_KSU_SUSFS_SUS_MAP=y' \
            'CONFIG_KSU_SUSFS_SUS_MOUNT=y' \
            'CONFIG_KSU_SUSFS_SUS_PATH=y'; do
            test "$(grep -Fxc "$line" "$config")" -eq 1
          done

          for build_file in common/BUILD.bazel msm-kernel/BUILD.bazel; do
            if [ -f "$build_file" ]; then
              sed -i '/"protected_exports_list"[[:space:]]*:[[:space:]]*"android\/abi_gki_protected_exports_aarch64",/d' "$build_file"
            fi
          done
          rm -f common/android/abi_gki_protected_exports_*
          rm -f msm-kernel/android/abi_gki_protected_exports_*

          # Keep the device's gb2a876903b49 base identifier without a -dirty
          # suffix after applying the audited local patches.
          sed -i "s/printf '%s' -dirty/: # reproducible patched tree/" common/scripts/setlocalversion

      - name: Build kernel
        run: |
          set -euo pipefail
          cd kernel_workspace
          mkdir -p "out/target/product/$CODENAME"
          SYSTEM_DLKM_RE_SIGN=0 \
          BUILD_SYSTEM_DLKM=0 \
          KMI_SYMBOL_LIST_STRICT_MODE=0 \
            ./kernel_platform/oplus/build/oplus_build_kernel.sh "$CODENAME" "$BUILD_VARIANT"

          # oplus_build_kernel.sh pipes the inner build through tee and can
          # otherwise return success even when Bazel failed. A fresh Actions
          # workspace must contain both of these outputs before we continue.
          dist="kernel_platform/out/msm-kernel-${CODENAME}-${BUILD_VARIANT}/dist"
          if [ ! -s "$dist/Image" ]; then
            echo "::error::Kernel build produced no non-empty dist/Image; refusing to continue."
            exit 1
          fi
          if [ ! -s "$dist/out_dir/.config" ]; then
            echo "::error::Kernel build produced no effective dist/out_dir/.config; refusing to continue."
            exit 1
          fi

      - name: Export effective kernel configuration
        run: |
          set -euo pipefail
          cd kernel_workspace/kernel_platform

          # OnePlus prepare_vendor.sh explicitly publishes the Kleaf output
          # tree as dist/out_dir and copies out_dir/.config as a build artifact.
          grep -Fq '"out_dir/.config"' build/kernel/android/prepare_vendor.sh
          effective_config_source="out/msm-kernel-${CODENAME}-${BUILD_VARIANT}/dist/out_dir/.config"
          test -s "$effective_config_source"
          cp "$effective_config_source" "$GITHUB_WORKSPACE/effective_kernel_config.txt"
          cmp -s "$effective_config_source" "$GITHUB_WORKSPACE/effective_kernel_config.txt"

      - name: Verify effective kernel configuration
        run: |
          set -euo pipefail

          effective_config="$GITHUB_WORKSPACE/effective_kernel_config.txt"
          test -s "$effective_config"

          stock_config="$GITHUB_WORKSPACE/baselines/PLK110_16.0.8.302_stock.config"
          expected_additions="$GITHUB_WORKSPACE/baselines/PLK110_16.0.8.302_expected_sukisu_susfs.config.additions"
          echo "$STOCK_CONFIG_SHA256  $stock_config" | sha256sum --check
          echo "$EXPECTED_CONFIG_ADDITIONS_SHA256  $expected_additions" | sha256sum --check

          verification_failed=0

          for symbol in \
            CONFIG_KSU \
            CONFIG_KSU_SUSFS \
            CONFIG_KSU_SUSFS_SUS_PATH \
            CONFIG_KSU_SUSFS_SUS_MOUNT \
            CONFIG_KSU_SUSFS_SUS_KSTAT \
            CONFIG_KSU_SUSFS_SPOOF_UNAME \
            CONFIG_KSU_SUSFS_HIDE_KSU_SUSFS_SYMBOLS \
            CONFIG_KSU_SUSFS_SPOOF_CMDLINE_OR_BOOTCONFIG \
            CONFIG_KSU_SUSFS_OPEN_REDIRECT \
            CONFIG_KSU_SUSFS_SUS_MAP; do
            if ! grep -qx "${symbol}=y" "$effective_config"; then
              echo "::error::Effective config does not enable ${symbol}=y"
              grep -E "^${symbol}=|^# ${symbol} is not set$" "$effective_config" || true
              verification_failed=1
            fi
          done

          require_exact_config_line() {
            local line="$1"
            local symbol="${2:-}"
            if ! grep -Fqx "$line" "$effective_config"; then
              echo "::error::Effective config missing required line: $line"
              if [ -n "$symbol" ]; then
                grep -E "^${symbol}=|^# ${symbol} is not set$" "$effective_config" || true
              fi
              verification_failed=1
            fi
          }

          require_exact_config_line '# CONFIG_KSU_FEATURE_ADBROOT is not set' CONFIG_KSU_FEATURE_ADBROOT
          require_exact_config_line '# CONFIG_KPM is not set' CONFIG_KPM
          require_exact_config_line '# CONFIG_KSU_DEBUG is not set' CONFIG_KSU_DEBUG
          require_exact_config_line '# CONFIG_KSU_SUSFS_ENABLE_LOG is not set' CONFIG_KSU_SUSFS_ENABLE_LOG
          require_exact_config_line 'CONFIG_LTO_NONE=y' CONFIG_LTO_NONE
          require_exact_config_line '# CONFIG_LTO_CLANG_FULL is not set' CONFIG_LTO_CLANG_FULL
          require_exact_config_line '# CONFIG_LTO_CLANG_THIN is not set' CONFIG_LTO_CLANG_THIN

          # A full diff is retained for review. Changes to scheduling, CPU
          # frequency/idle, ZRAM, LTO/CFI, module ABI/signing, preemption or
          # debug hardening are fatal: this kernel is an A.67 stock-behaviour
          # baseline with only SukiSU/SUSFS integration.
          export LC_ALL=C
          grep -E '^(CONFIG_|# CONFIG_.* is not set)' "$stock_config" | sort -u > /tmp/stock.config.sorted
          grep -E '^(CONFIG_|# CONFIG_.* is not set)' "$effective_config" | sort -u > /tmp/effective.config.sorted
          diff -u /tmp/stock.config.sorted /tmp/effective.config.sorted > "$GITHUB_WORKSPACE/kernel_config_delta.txt" || true

          # Full-diff gate: the effective build may only add the exact pinned
          # SukiSU/SUSFS symbols below and may not remove any stock A.67 symbol.
          comm -13 /tmp/stock.config.sorted /tmp/effective.config.sorted > /tmp/actual.config.additions
          comm -23 /tmp/stock.config.sorted /tmp/effective.config.sorted > /tmp/unexpected.config.removals

          if [ -s /tmp/unexpected.config.removals ]; then
            echo "::group::Unexpected stock config removals"
            cat /tmp/unexpected.config.removals
            echo "::endgroup::"
            echo "::error::Effective config removed stock A.67 symbols."
            verification_failed=1
          fi

          if ! diff -u "$expected_additions" /tmp/actual.config.additions > "$GITHUB_WORKSPACE/config_additions_diff.txt"; then
            echo "::group::Expected SukiSU/SUSFS config additions"
            cat "$expected_additions"
            echo "::endgroup::"
            echo "::group::Actual config additions"
            cat /tmp/actual.config.additions
            echo "::endgroup::"
            echo "::group::Config additions diff"
            cat "$GITHUB_WORKSPACE/config_additions_diff.txt"
            echo "::endgroup::"
            echo "::error::Effective config additions differ from the audited SukiSU/SUSFS set."
            verification_failed=1
          fi

          sensitive='^(# )?CONFIG_(PREEMPT.*|HZ.*|SCHED.*|UCLAMP.*|WALT.*|HMBIRD.*|SLIM_SCHED.*|EXT_GROUP_SCHED.*|CPU_FREQ.*|CPU_IDLE.*|ZRAM.*|ZSMALLOC.*|LTO.*|CFI.*|MODVERSIONS|MODULE_SIG.*|KASAN.*|DEBUG_KERNEL)(=| is not set)'
          grep -E "$sensitive" "$stock_config" | sort > /tmp/stock.sensitive
          grep -E "$sensitive" "$effective_config" | sort > /tmp/effective.sensitive
          if ! diff -u /tmp/stock.sensitive /tmp/effective.sensitive > "$GITHUB_WORKSPACE/sensitive_config_diff.txt"; then
            echo "::group::Sensitive config diff"
            cat "$GITHUB_WORKSPACE/sensitive_config_diff.txt"
            echo "::endgroup::"
            echo "::error::Sensitive stock kernel configuration changed."
            verification_failed=1
          fi

          cp "$stock_config" "$GITHUB_WORKSPACE/stock_kernel_config.txt"
          cp "$expected_additions" "$GITHUB_WORKSPACE/expected_config_additions.txt"
          cp /tmp/actual.config.additions "$GITHUB_WORKSPACE/actual_config_additions.txt"
          cp /tmp/unexpected.config.removals "$GITHUB_WORKSPACE/unexpected_config_removals.txt"

          if [ "$verification_failed" -ne 0 ]; then
            echo "::error::Effective kernel configuration verification failed; see diagnostics above."
            exit 1
          fi

      - name: Package kernel and stable userspace module separately
        run: |
          set -euo pipefail

          dist=kernel_workspace/kernel_platform/out/msm-kernel-canoe-perf/dist
          image="$dist/Image"
          test -s "$image"

          strings "$image" > /tmp/plk110-image.strings
          kernel_banner="$(grep -m1 '^Linux version ' /tmp/plk110-image.strings)"
          test -n "$kernel_banner"
          printf '%s\n' "$kernel_banner" | grep -F "$EXPECTED_KERNEL_RELEASE"
          ! printf '%s\n' "$kernel_banner" | grep -F -- '-dirty'

          git clone --filter=blob:none --no-checkout --depth=1 --branch master \
            https://github.com/Numbersf/AnyKernel3.git AnyKernel3
          git -C AnyKernel3 fetch --depth=1 origin "$ANYKERNEL_COMMIT"
          git -C AnyKernel3 checkout --detach "$ANYKERNEL_COMMIT"
          test "$(git -C AnyKernel3 rev-parse HEAD)" = "$ANYKERNEL_COMMIT"
          rm -rf AnyKernel3/.git

          cp "$image" AnyKernel3/Image
          sed -i 's/^kernel.string=.*/kernel.string=PLK110 SukiSU 4.2 + SUSFS 2.3/' AnyKernel3/anykernel.sh
          sed -i 's/^do.devicecheck=.*/do.devicecheck=1/' AnyKernel3/anykernel.sh
          sed -i 's/^device.name1=.*/device.name1=PLK110/' AnyKernel3/anykernel.sh
          sed -i 's/^device.name2=.*/device.name2=OP60FFL1/' AnyKernel3/anykernel.sh
          sed -i 's/^PATCH_VBMETA_FLAG=.*/PATCH_VBMETA_FLAG=0/' AnyKernel3/anykernel.sh

          grep -qx 'BLOCK=boot' AnyKernel3/anykernel.sh
          grep -qx 'IS_SLOT_DEVICE=auto' AnyKernel3/anykernel.sh
          grep -qx 'PATCH_VBMETA_FLAG=0' AnyKernel3/anykernel.sh
          ! grep -Eq '^[[:space:]]*(patch_fstab|flash_generic|write_boot.*vendor)' AnyKernel3/anykernel.sh
          for forbidden in \
            dtb dtb.img dtbo dtbo.img recovery_dtbo recovery_dtbo.img \
            vendor_boot vendor_boot.img init_boot init_boot.img vbmeta vbmeta.img \
            vendor_kernel_boot vendor_kernel_boot.img \
            vendor_dlkm vendor_dlkm.img system_dlkm system_dlkm.img \
            recovery recovery.img abl abl.img; do
            test ! -e "AnyKernel3/$forbidden"
          done
          test ! -d AnyKernel3/vendor_ramdisk
          test ! -d AnyKernel3/vendor_patch
          test ! -d AnyKernel3/modules

          mkdir artifacts
          (
            cd AnyKernel3
            zip -r9 ../artifacts/PLK110_SukiSU-4.2_SUSFS-2.3_NoKPM.zip .
          )

          curl --fail --location --retry 3 \
            "$SUSFS_MODULE_URL" \
            --output artifacts/ksu_module_susfs_1.5.2+_R28.zip
          echo "$SUSFS_MODULE_SHA256  artifacts/ksu_module_susfs_1.5.2+_R28.zip" | sha256sum --check

          image_sha="$(sha256sum "$image" | awk '{print $1}')"
          kernel_zip_sha="$(sha256sum artifacts/PLK110_SukiSU-4.2_SUSFS-2.3_NoKPM.zip | awk '{print $1}')"
          module_sha="$(sha256sum artifacts/ksu_module_susfs_1.5.2+_R28.zip | awk '{print $1}')"

          {
            echo "device=$DEVICE"
            echo "firmware_baseline=$FIRMWARE_BASELINE"
            echo "expected_kernel_release=$EXPECTED_KERNEL_RELEASE"
            echo "stock_kernel_build_number=$BUILD_NUMBER"
            echo "manifest=$MANIFEST_COMMIT"
            echo "common=$COMMON_COMMIT"
            echo "soc=$SOC_COMMIT"
            echo "modules=$MODULES_COMMIT"
            echo "sukisu=$SUKISU_COMMIT"
            echo "susfs=$SUSFS_COMMIT"
            echo "anykernel=$ANYKERNEL_COMMIT"
            echo "kpm=disabled"
            echo "susfs_log=disabled"
            echo "image_sha256=$image_sha"
            echo "kernel_zip_sha256=$kernel_zip_sha"
            echo "module_sha256=$module_sha"
          } > artifacts/BUILD_INFO.txt

          cp "$image" artifacts/Image
          cp "$GITHUB_WORKSPACE/effective_kernel_config.txt" artifacts/
          cp stock_kernel_config.txt artifacts/
          cp expected_config_additions.txt artifacts/
          cp actual_config_additions.txt artifacts/
          cp unexpected_config_removals.txt artifacts/
          cp kernel_config_delta.txt artifacts/
          printf '%s\n' "$kernel_banner" > artifacts/KERNEL_BANNER.txt
          (
            cd artifacts
            sha256sum \
              BUILD_INFO.txt \
              Image \
              KERNEL_BANNER.txt \
              PLK110_SukiSU-4.2_SUSFS-2.3_NoKPM.zip \
              actual_config_additions.txt \
              effective_kernel_config.txt \
              expected_config_additions.txt \
              kernel_config_delta.txt \
              ksu_module_susfs_1.5.2+_R28.zip \
              stock_kernel_config.txt \
              unexpected_config_removals.txt \
              > SHA256SUMS.txt
          )

      - name: Upload verified build artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: PLK110-SukiSU-4.2-SUSFS-2.3-verified
          path: artifacts/*
          if-no-files-found: error
          compression-level: 0
          retention-days: 14